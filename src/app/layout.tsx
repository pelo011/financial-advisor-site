import type { Metadata } from 'next'
import { Cormorant_Garamond, DM_Sans } from 'next/font/google'
import './globals.css'

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  style: ['normal', 'italic'],
  variable: '--font-cormorant',
  display: 'swap',
})

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-dm-sans',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Patrik Géci | Financial Advisor',
  description:
    'Personalized wealth management, retirement planning, and investment strategy for individuals, families, and businesses. Schedule a consultation today.',
  keywords: ['financial advisor', 'wealth management', 'retirement planning', 'investment strategy'],
  openGraph: {
    title: 'Patrik Géci | Financial Advisor',
    description: 'Trusted financial advisory services for individuals and businesses.',
    type: 'website',
  },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${cormorant.variable} ${dmSans.variable}`}>
      <body>{children}</body>
    </html>
  )
}